# Second-environment benchmark evidence

Run `python benchmark_system.py` on a second computer or operating system, then rename the generated
JSON file to identify that environment and keep it in this folder.

Record:

- device/model:
- operating system:
- Python version:
- processor and logical CPU count:
- available memory:
- browser and version:
- Ollama enabled or fallback mode:
- date:

| Runtime path | Median (ms) | P95 (ms) | Result |
|---|---:|---:|---|
| Crisis safety routing | | | |
| Greeting | | | |
| English retrieval | | | |
| Kinyarwanda retrieval | | | |
| Guided check-in | | | |

Attach screenshots of the benchmark output and application running in the recorded environment. Do
not invent values if a second device is unavailable; state that the evaluation remains pending.

# Hugging Face Space: fine-tuned generator inference API only.
# Deliberately separate from the main repo's requirements*.txt -- this runs
# in its own Gradio+ZeroGPU Space, not Vercel's serverless function.
spaces
torch>=2.2
transformers>=4.46,<6
peft>=0.13

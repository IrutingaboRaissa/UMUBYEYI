# Pin a light theme so the UI (and button hover) renders the same for every tester,
# independent of their OS / browser dark-mode setting.
[theme]
base = "light"
primaryColor = "#C9719E"
backgroundColor = "#FBEFFB"
secondaryBackgroundColor = "#F6F2FB"
textColor = "#4A3B57"
font = "sans serif"

# Vercel Python functions. Keep this root file lightweight: Vercel discovers and
# installs it automatically for every api/*.py function.
scikit-learn==1.9.0
joblib>=1.3
numpy>=2.0
pandas>=2.2
SQLAlchemy>=2.0
psycopg2-binary>=2.9
shap>=0.52
# Pinned explicitly: shap's own unconstrained "numba"/"llvmlite" deps let some resolvers
# (seen on Vercel's uv-based build) land on ancient pre-built versions with no Python 3.12
# wheel, forcing a source build that fails. Pinning modern versions here wins resolution.
numba>=0.60
llvmlite>=0.43

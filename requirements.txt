# Umubyeyi runtime. FULLY LOCAL: no external LLM API.
# Product = grounded generation (RAG): retrieve validated postpartum snippets, then OUR
# fine-tuned flan-t5 generator answers (retrieval fallback if unavailable). Frontend: Streamlit.

# retrieval / grounding / router (our own sklearn models)
scikit-learn==1.9.0
joblib==1.5.2
numpy==2.4.6

# generation (our own fine-tuned generator — no commercial API)
transformers>=4.44
torch>=2.2
sentencepiece>=0.2

# backend + frontend
fastapi>=0.115
uvicorn[standard]>=0.30
streamlit==1.48.1
streamlit-local-storage==0.0.25   # on-device (browser) conversation memory

# analytics / feedback datastore (Postgres in prod via DATABASE_URL, SQLite locally)
SQLAlchemy>=2.0
psycopg2-binary>=2.9

# pilot feedback -> Google Sheet
gspread>=6.0
google-auth>=2.30

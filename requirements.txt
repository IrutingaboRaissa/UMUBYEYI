# Umubyeyi runtime.
# Product = grounded generation (RAG): retrieve validated MOTHER+KB snippets, then an LLM
# (Gemini) answers in the user's language. Backend: FastAPI. Frontend: Streamlit.
# The LLM key is read from .env (GEMINI_API_KEY) and never committed.

# retrieval / grounding
scikit-learn==1.9.0
joblib==1.5.2
numpy==2.4.6

# generation
google-genai>=1.0
python-dotenv>=1.0

# backend + frontend
fastapi>=0.115
uvicorn[standard]>=0.30
streamlit==1.48.1
streamlit-local-storage==0.0.25   # on-device (browser) conversation memory

# analytics / feedback datastore (Postgres in prod via DATABASE_URL, SQLite locally)
SQLAlchemy>=2.0
psycopg2-binary>=2.9

# pilot feedback -> Google Sheet
gspread>=6.0
google-auth>=2.30
